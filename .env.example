# Supabase — get these from your project's API settings (https://app.supabase.com → Project Settings → API)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Active edition (uuid) — optional. If unset, the app uses the most recent edition row.
# VITE_EDITION_ID=
